Dropzones = (function() {
    var e = $('[data-toggle="dropzone"]'),
        i = $(".dz-preview");
    e.length && (Dropzone.autoDiscover = !1, e.each((function() {
        var e, n, t, o, l;
        e = $(this), n = void 0 !== e.data("dropzone-multiple"), t = e.find(i), o = void 0, l = {
            url: e.data("dropzone-url"),
            thumbnailWidth: null,
            thumbnailHeight: null,
            previewsContainer: t.get(0),
            previewTemplate: t.html(),
            maxFiles: n ? null : 1,
            acceptedFiles: n ? null : "image/*",
            success: function(file,response){
                response = JSON.parse(response);
                if (response.status == 1) {
                    $("#pr_image_db").html('');
                    $(".update_img_hidden").val(response.message);
                } else {
                    $(".dz-preview .dz-preview-img").attr({ 'src': "", 'alt': "" });
                    Notification('danger', 'Error', `${response.message}`);
                }
            },
            init: function() {
                this.on("addedfile", (function(e) {
                    !n && o && this.removeFile(o), o = e
                }))
            }
        }, t.html(""), e.dropzone(l)
    })))
})();